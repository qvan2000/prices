server:
  port: 9285

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  flyway:
    baseline-on-migrate: true
    baseline-version: 1_0_0
    schemas: public
    create-schemas: true
    default-schema: public